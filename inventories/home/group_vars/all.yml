---
# Defaults shared across all machines.

bootstrap_git_user_name: "HexSleeves"
bootstrap_git_user_email: "lecoqjacob@gmail.com"

# Profile hint for roles to make safe decisions.
# Supported values: desktop, vm
os_linux_machine_profile: desktop

# Core directories to ensure exist.
bootstrap_common_directories:
  - .config
  - .local/bin
  - projects
  - workspace

# Chezmoi settings.
chezmoi_enabled: true
chezmoi_repo: ""
chezmoi_apply_force: false

# Tooling toggles.
tooling_mise_enabled: true
tooling_bun_enabled: true
